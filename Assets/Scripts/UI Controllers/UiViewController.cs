using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UiViewController : UiController
{
    // private void Start()
    // {
    //     SetState(STATE.READY_TO_START, forceState: true);
    // }

    // public void SetState(STATE newState, bool forceState = false)
    // {
    //     if (newState == state && !forceState) return;

    //     state = newState;
    //     print(this + ">>>> CAMBIATO STATO UI IN: " + state.ToString());

    //     switch (state)
    //     {
    //         case STATE.READY_TO_START:
    //             break;

    //         case STATE.IN_GAME:
    //             // GameManager.instance.SetGameRunning();
    //             break;

    //         // case STATE.FINISHED_FOR_ME:
    //         //     break;

    //         // case STATE.FINISHED_FOR_ALL:
    //         //     break;
    //     }

    //     /// Setup Panel UI
    //     Set_PanelsUI_on_STATE();
    // }
}
